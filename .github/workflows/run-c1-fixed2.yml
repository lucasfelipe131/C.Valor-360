name: Run final C1 copy review

on:
  push:
    branches: [c1-ptbr-copy-review]
    paths:
      - scripts/run-c1-fixed2.sh
      - .github/workflows/run-c1-fixed2.yml

permissions:
  contents: write

jobs:
  apply:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: c1-ptbr-copy-review
          fetch-depth: 0
      - name: Aplicar revisão final
        run: bash scripts/run-c1-fixed2.sh
      - name: Registrar mudanças
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "C1 — revisa ortografia, gramática e pontuação dos textos humanos"
          git push origin HEAD:c1-ptbr-copy-review
