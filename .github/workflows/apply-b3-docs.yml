name: Apply B3 async docs

on:
  push:
    branches: [b3-shared-async-resource]
    paths:
      - scripts/apply-b3-docs.py
      - .github/workflows/apply-b3-docs.yml

permissions:
  contents: write

jobs:
  apply:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: b3-shared-async-resource
          fetch-depth: 0
      - name: Aplicar documentação
        run: python3 scripts/apply-b3-docs.py
      - name: Registrar mudanças
        run: |
          rm -f scripts/apply-b3-docs.py .github/workflows/apply-b3-docs.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "B3 — documenta o estado assíncrono compartilhado"
          git push origin HEAD:b3-shared-async-resource
